# Optional. Leave empty to use same-origin /api (the Vite dev server proxies
# /api to the local Worker, and Cloudflare Pages can be configured the same way).
# Set this when the API worker is deployed to a different origin.
VITE_API_URL=
